# Generated runtime resources

`pi-runtime/` is generated by the platform staging scripts before packaging.
It contains the pinned Pi runtime, frozen finance worker, and hash-locked
Deno/Pyodide compute worker, so it must not be committed or edited as source.
